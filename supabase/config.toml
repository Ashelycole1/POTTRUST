# ─────────────────────────────────────────────────────────────────────────────
# Supabase Project Config
# Supabase CLI uses this file to apply migrations from the supabase/migrations/
# folder automatically when you run `supabase db push` or link to GitHub CI.
# ─────────────────────────────────────────────────────────────────────────────

project_id = "lohdeymkknsujltishpw"

[api]
enabled = true
port = 54321
schemas = ["public", "storage"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[studio]
enabled = true
port = 54323

[inbucket]
enabled = true
port = 54324

[storage]
enabled = true
file_size_limit = "5MiB"

[auth]
enabled = true
site_url = "http://localhost:5173"
additional_redirect_urls = ["https://pottrust.vercel.app"]
jwt_expiry = 3600
enable_refresh_token_rotation = true
refresh_token_reuse_interval = 10

[analytics]
enabled = false
